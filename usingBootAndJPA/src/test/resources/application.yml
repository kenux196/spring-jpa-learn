# 테스트는 테스트 전용 application.yml 사용할 것.

# 테스트에서는 기본적으로 스프링부트에서 제공하는 메모리 데이터베이스 사용된다.
#spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#    org.hibernate.type: trace